[DB]
dialect = mysql+mysqlconnector
host = {{site.local.mysql.host}}
port = {{site.local.mysql.port}}
user = {{site.local.mysql.user}}
password = {{site.local.mysql.password}}
db = {{site.local.mysql.db}}
[LOCAL_RPC_SERVER]
user = {{site.local.rmq.local.user}}
password = {{site.local.rmq.local.password}}
host = {{site.local.rmq.local.host}}
port = {{site.local.rmq.local.port}}
vhost = {{site.local.rmq.local.vhost}}
queue = {{site.local.rmq.local.queue}}
num_threads = 5
max_retries = 3
[CENTRAL_RPC_SERVER]
user = {{site.local.rmq.central.user}}
password = {{site.local.rmq.central.password}}
host = {{site.local.rmq.central.host}}
port = {{site.local.rmq.central.port}}
vhost = {{site.local.rmq.central.vhost}}
queue = {{site.local.rmq.central.queue}}
num_threads = 5
max_retries = 3
[CENTRAL_RPC_CLIENT]
user = {{central.rmq.user}}
password = {{central.rmq.password}}
host = {{central.rmq.host}}
port = {{central.rmq.port}}
vhost = {{central.rmq.vhost}}
queue = {{central.rmq.queue}}
[GLOBUS]
client_id = {{site.local.globus.client_id}}
endpoint_id = {{site.local.globus.endpoint_id}}
root_dir = {{site.local.globus.root_dir}}
default_dir = {{site.local.globus.default_dir}}
[SITE]
id = {{site.local.name}}
uploads_subdirectory = {{site.local.globus.uploads_subdir}}
downloads_subdirectory = {{site.local.globus.downloads_subdir}}
[CROMWELL]
url = {{site.local.cromwell.url}}:{{site.local.cromwell.deploy_name.port}}

