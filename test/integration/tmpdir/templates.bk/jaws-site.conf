[DB]
dialect = mysql+mysqlconnector
host = {{site.local.mysql.host}}
port = {{site.local.mysql.port}}
user = {{site.local.mysql.user}}
password = {{site.local.mysql.password}}
db = {{site.local.mysql.db}}
[LOCAL_RPC_SERVER]
user = {{site.local.rmq.local.user}}
password = {{site.local.rmq.local.password}}
host = {{site.local.rmq.local.host}}
port = {{site.local.rmq.local.port}}
vhost = {{site.local.rmq.local.vhost}}
queue = {{site.local.rmq.local.queue}}
num_threads = 5
max_retries = 3
[CENTRAL_RPC_SERVER]
user = {{site.local.rmq.central.server.user}}
password = {{site.local.rmq.central.server.password}}
host = {{site.local.rmq.central.server.host}}
port = {{site.local.rmq.central.server.port}}
vhost = {{site.local.rmq.central.server.vhost}}
queue = {{site.local.rmq.central.server.queue}}
num_threads = 5
max_retries = 3
[CENTRAL_RPC_CLIENT]
user = {{site.local.rmq.central.client.user}}
password = {{site.local.rmq.central.client.password}}
host = {{site.local.rmq.central.client.host}}
port = {{site.local.rmq.central.client.port}}
vhost = {{site.local.rmq.central.client.vhost}}
queue = {{site.local.rmq.central.client.queue}}
[GLOBUS]
client_id = {{site.local.globus.client_id}}
endpoint_id = {{site.local.globus.endpoint_id}}
root_dir = /
default_dir = ~
[SITE]
id = {{site.local.name}}
staging_subdirectory = {{site.local.globus.staging_subdir}}
results_subdirectory = {{site.local.globus.results_subdir}}
[CROMWELL]
url = {{site.local.cromwell.url}}:{{site.local.cromwell.port}}
