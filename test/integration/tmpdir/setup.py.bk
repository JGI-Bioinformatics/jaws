from setuptools import setup
import os

setup(
    name="deploy_jaws",
    version=os.popen("git describe --dirty=-dev --always --tags --abbrev=6")
    .read()
    .strip()
    .replace("-", "+", 1),
    description="JGI Analysis Workflow Service Site Server",
    url="https://gitlab.com/jgi-dsi/aa/jaws/test/integration/deploy",
    author="The JAWS Team",
    packages=["deploy-jaws"],
    install_requires=[line.strip() for line in open("requirements.txt")],
    entry_points={"console_scripts": ["deploy-jaws = deploy-jaw.deploy:main", ]},
    zip_safe=False,
)
